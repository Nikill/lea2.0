<ul id="main-menu" class="main-menu">
    <li class="root-level">
        <a href="{{ path('homepage') }}">
            <i class="entypo-home"></i>
            <span class="title">Accueil</span>
        </a>
    </li>

    {% if is_granted('ROLE_SUPER_ADMIN') %}
        <li class="root-level">
            <a href="{{ path('user_index') }}">
                <i class="entypo-users"></i>
                <span class="title">Utilisateurs</span>
            </a>
        </li>
    {% endif %}

    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <li class="root-level">
            <a href="{{ path('questionnaire_index') }}">
                <i class="entypo-search"></i>
                <span class="title">Questionnaires</span>
            </a>
        </li>
    {% endif %}

    {% if is_granted('ROLE_RESPONSABLE') %}
        <li class="root-level">
            <a href="{{ path('typeDocument_index') }}">
                <i class="entypo-docs"></i>
                <span class="title">Types de document</span>
            </a>
        </li>
    {% endif %}

    {% if is_granted('ROLE_USER') %}
        <li class="root-level">
            <a href="{{ path('document_index') }}">
                <i class="entypo-docs"></i>
                <span class="title">Documents</span>
            </a>
        </li>
    {% endif %}
    {% if 'ROLE_ETUDIANT' == app.session.get('roleUser') %}
    <div id="contactsSideBar">
        <i class="entypo-info-circled"></i>
        <span class="title">Contacts</span><br><br>
        <span class="title">Maître d'apprentissage <br>
        {{app.session.get('map').prenom}} {{app.session.get('map').nom}}<br>
        {{app.session.get('map').email}}
            </span>
        <br><br>
        <span class="title">Tuteur <br>
        {{app.session.get('tuteur').prenom}} {{app.session.get('tuteur').nom}}<br>
        {{app.session.get('tuteur').email}}
            </span>
    </div>
{% endif %}
</ul>