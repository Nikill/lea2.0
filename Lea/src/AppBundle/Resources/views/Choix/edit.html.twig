{% extends "AppBundle::base.html.twig" %}

{% block body %}
    <div class="col-md-offset-3 col-md-6">
        <div class="block">
            <div class="block-title">
                <h2>Éditer un choix</h2>
            </div>
            {{ form_start(formChoix, { 'attr': { 'class': 'form-horizontal form-bordered'} }) }}
                <div class="form-group">
                    <div class="col-md-3 control-label">
                        {{ form_label(formChoix.description) }}
                        {{ form_widget(formChoix.description) }}
                    </div>
                    <div class="col-md-9">
                        {{ form_label(formChoix.rang) }}
                        {{ form_widget(formChoix.rang) }}
                    </div>
                </div>
                <div class="form-group form-actions">
                    <div class="col-md-9 col-md-offset-3">
                        {{ form_widget(formChoix.save, { 'attr': { 'class': 'btn btn-sm btn-primary'} }) }}
                    </div>
                </div>
            {{ form_rest(formChoix) }}
            {{ form_end(formChoix) }}
        </div>
    </div>
{% endblock %}